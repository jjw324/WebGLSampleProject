export default class Light {
    constructor(position, ambient, diffuse, specular, shine) {
        this.position = position
        this.ambient = ambient
        this.diffuse = diffuse
        this.specular = specular
        this.shine = shine
    }
}
